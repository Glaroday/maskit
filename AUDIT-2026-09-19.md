# Maskit 全链路审计报告

- **审计对象**：浏览器扩展链路 · 内置模型（NER）识别 · API 调用与安全审计模块
- **审计日期**：2026-09-19
- **审计基线**：工作区未提交改动（`git diff` = 2571 insertions / 308 deletions，`v0.2.12` 之后的全部开发）
- **审计方式**：静态读码 + 隔离数据目录实机复现（`LLM_SHIELD_DATA_DIR` 指向临时目录，**未触碰本机真实事件库**）
- **门禁结果**：`scripts/verify-all.py` **15/15 全部通过**，单测 824 项 OK（2 skipped）

> ⚠️ **基线漂移声明**：审计期间 `engine/ner_engine.py`（252 → 405 行）、`engine/transparent.py`、`engine/panel.py`、
> `engine/maskit-engine.spec`、`requirements-dev.txt` 均被**实时改写**（12:28 → 12:39）。
> 本报告所有结论已按**最终版本重新核验**；NER 与打包相关的若干问题在审计过程中已被修掉，不再列入。

---

## 一、结论速览

**不能发版。** 门禁全绿，但门禁覆盖不到本次发现的 4 项阻断级缺陷。

| # | 缺陷 | 级别 | 证据强度 |
|---|---|---|---|
| B1 | 扩展链路把**未脱敏原文 / 已还原明文**写入 SQLite，且经 `/api/logs` 可直接读回 | **阻断**（凭据红线） | 实机 PoC |
| B2 | **数值型 / 键名 / 重复键**敏感值漏检 → 明文出网，fail-closed 兜不住 | **阻断** | 实机 PoC |
| B3 | 扩展「总开关」`enabled` 不参与数据链路，UI 谎报「已停用」 | **阻断**（界面承诺 ≠ 行为） | 代码确证 |
| B4 | 换芯检测被 `text_chunks` 门控 → tool_use / reasoning-only / 空文本响应上**完全失效** | **阻断** | 函数级实测 |
| M1 | PEM 正则 O(n²) + 审计路径无 body 截断 → 恶意上游可卡死代理 | 中危 | 实测耗时曲线 |
| M2 | `/api/ext/restore` 缺体积闸门，`ext_frames` 无 stream_id 上限 | 中危 | 代码确证 |
| M3 | 扩展令牌可被发往任意外域（SW 侧零 host 校验） | 中危 | 代码确证 |
| M4 | ≤80 字符的写请求**整体跳过打码**且无任何记录 | 中危 | 代码确证 |
| M5 | 24–25MB Office 附件必然硬失败（base64 膨胀越过 32MB 闸门） | 中危 | 算术+代码 |
| M6 | NER 部分结果被写入 LRU 缓存 → 该文本持续欠脱敏 | 中危 | 代码确证 |
| M7 | NER 的「超长整条跳过」在 UI 不可见 | 中危 | 代码确证 |

**先修 B1–B4，再谈发版。**

---

## 二、阻断级缺陷（含复现证据）

### B1 扩展链路凭据明文落库，且可经 `/api/logs` 读回 —— 违反项目自定红线

**代码位置**

- `engine/panel.py:5711-5717`（`/api/ext/mask`）：
  ```python
  if _ext_cfg().get("ext_record_events", True) and hit_count > 0:
      tr._emit("MASK", ingress="ext", sid=sid, ..., items=items,
               dialog=text[:4000],        # ← text 是客户端原始未脱敏请求体
               req_preview=text[:800], ...)
  ```
- `engine/panel.py:5947-5959`（`/api/ext/restore`）：`dialog=(out[:4000])` / `resp_preview=out[:800]`，`out` 是**已还原**明文。
- 对照代理链路：`engine/transparent.py:4326-4327`、`5373-5374` 在 `_emit` 前强制
  `_redact_credentials(...)` / `_redact_session_credentials(...)`。**扩展链路这两处一次都没调用。**
- `_emit`（`transparent.py:3532`）自身不做任何清洗。

**实机复现**（隔离库，伪造凭据 `sk-test-0000-…`）

```
[1] /api/ext/mask  → masked_text: {"content":"我的手机 {{PHONE_nfmrgc}}，key 是 {{APIKEY_gptxwf}}"}
    事件库 MASK payload：
      dialog      : 含明文 key = True   '{"content": "key 是 sk-test-0000-aaaaaaaaaaaaaaaaaaaaaaaa"}'
      req_preview : 含明文 key = True
      items       : [{'label':'API_KEY','preview':'sk-…aaaa','cred':True,'digest':'f1e70f97…'}]  ← items 本身合规
[2] /api/ext/restore
    事件库 RESTORE payload：
      dialog       : 含明文 key = True（**已还原**的真值）
      resp_preview : 含明文 key = True
[3] GET /api/logs（非 slim）：200，含明文 key = True，含手机号 = True
    GET /api/logs?slim=1   ：200，含明文 key = False
```

**为什么是阻断**：`items` 严格守着「凭据只存 digest + preview」，但同一个事件对象的 `dialog` / `*_preview`
把**完整凭据真值**一起写进了 SQLite，且默认开关 `ext_record_events=True`（`panel.py:3422/3827`）。
读侧 `api_logs()`（`panel.py:5287`）在非 slim 分支不做 `_scrub_legacy_event`（只有 `/api/logs/detail` 做了）。
结果是：**任何持 `API_TOKEN` 的调用方、以及诊断包/事件库文件本身，都能捞回 API Key 原文** ——
这正是项目红线「凭据类永远无法从 SQLite 回溯」所禁止的形态。

**修复面很小**：两处 `_emit` 前补上与代理链路同源的清洗（RESTORE 再加 `_redact_session_credentials`），
并让 `api_logs` 非 slim 分支补读侧兜底。

---

### B2 数值型 / 键名 / 重复键敏感值漏检 —— 明文出网，且 fail-closed 兜不住

**代码位置**：`engine/transparent.py:3357`

```python
    if isinstance(obj, dict):
        ...
        return {k: _mask_tree(v, sid, k, key, path + (k,), depth + 1, flag) for k, v in obj.items()}
    return obj          # ← int / float / bool / None 一律原样返回，从不扫描
```
键名不脱敏是**注释里自认的有意边界**（3343-3356）；重复键在 `json.loads` 阶段已被后者覆盖。

**实机复现**（`tr.mask_body`，同一会话）

| 形态 | 输入 | 输出 | 明文残留 |
|---|---|---|---|
| 字符串手机号 | `{"phone": "13800138000"}` | `{"phone": "{{PHONE_thhpqz}}"}` | False ✅ |
| **数值型手机号** | `{"phone": 13800138000}` | `{"phone": 13800138000}` | **True** ❌ |
| **数值型身份证** | `{"id_card": 110101199001011234}` | 原样 | **True** ❌ |
| **敏感值作键名** | `{"13800138000": "safe"}` | 原样 | **True** ❌ |
| **重复键（敏感在前）** | `{"a":"13800138000","a":"safe"}` | 原样 | **True** ❌ |

**为什么是阻断**：三条路径**都不抛异常**，因此 `changed[0]` 为假 →
`mask_body` / `request()` 直接「一个字都不动」放行原文（`transparent.py:3457`、`4246-4251`）。
`FAIL_CLOSED 503` 只兜异常，**兜不住「静默判定为无需改写」**。
代理链路与扩展链路同时受影响，与扩展无关。

现实可达性：Agent 工具调用把结构化参数（`{"mobile": 138…}`、`{"id_card": …}`）以数字发送，
以及业务数据以手机号为字典键，都是常见形态。

---

### B3 扩展「总开关」不生效 —— 界面说关了，实际还在打码

**代码位置**

- `extension/background.js:491-497`（`handleMask`）与 `512-518`（`handleMaskFile`）**只查 `siteCovers(host, cfg.enabledSites)`**。
- 全文件对 `cfg.enabled` 的唯一引用是 `background.js:582`（快照，仅供展示）。
- `extension/bridge-main.js` 完全不读 `enabled`（已 grep 确认为空）。

**谎言的两处落点**

- `extension/popup.js:172`：`if (cfg.enabled === false) { kind='bad'; text=t('statusDisabledLocal') }` → 显示「已停用」。
- `extension/options.js:35` 文案：**「启用浏览器扩展链路（关闭后网页请求原样直连，引擎侧也会同步拒绝）」** ——
  扩展从未把该开关同步给引擎（引擎侧只有独立的 `ext_bridge_enabled`）。

**后果**：用户在设置页取消勾选并保存 → 内容脚本照常注入、`handleMask` 照常送原文打码、页面被改写，
而 popup 明确显示「扩展已停用」。可达性 100%，无需攻击者。

---

### B4 换芯检测被 `text_chunks` 门控 —— 旗舰能力在主要响应形态上失效

**代码位置**：`engine/transparent.py:3618-3619`

```python
text_chunks, model_field, events = _parse_response_payload(body_text, ct)
if AUDIT_SIGNALS.get("identity_swap"):
    ...
    for chunk in text_chunks:                       # ← 门控：文本块为空则整段跳过
        findings.extend(_audit.scan_identity_swap(chunk, model_field, req_model))
```

而 `scan_identity_swap` 的第三个参数（文本）**根本没被使用** —— 判定只需 `model_field` + `req_model`。

**函数级实测**

```
scan_identity_swap("", "gpt-4o-mini",      "gpt-4o")            → model_tier_mismatch  MEDIUM
scan_identity_swap("", "claude-3-5-sonnet","gpt-4o")            → model_mismatch       HIGH
```

**哪些响应会让 `text_chunks` 为空**（`_parse_response_payload` 只采集 `content[].text` / `choices[].delta.content`）：

- Anthropic 非流式 **tool_use-only** 响应（`content` 块只有 `tool_use`，无 `text` 键）
- Anthropic 流式 tool_use（载荷在 `delta.partial_json`，采集只认 `delta.text`）
- OpenAI 非流式 `content: null` 且无 `tool_calls`（拒答/空答）
- reasoning / thinking 通道；流被截断在首个 content 块之前

**后果**：编程助手（Claude Code / Cursor）大量响应是 tool_use / reasoning-only。
恶意 relay 只要返回 `{"model":"gpt-3.5","choices":[{"message":{"content":""}}]}` 即可**静默绕过**，
而 `model_field` 明明已经解析出来。

**测试把问题掩盖了**：`tests/test_regressions.py` 里写的是 `for chunk in chunks or [""]:`，
生产代码没有 `or [""]` —— 测试绿、生产漏。

---

## 三、中危缺陷

### M1 PEM 正则 O(n²)，且审计路径没有 body 截断 —— 恶意上游可卡死本地代理

`engine/audit_signals.py:49`：
```python
re.compile(r"-----BEGIN[A-Z \-]*PRIVATE KEY-----[\s\S]*?-----END[A-Z \-]*PRIVATE KEY-----")
```
`engine/transparent.py:3570 / 3592` 把**全量** body 交给它（512KB 上限只作用于另一条 `_scan_response`）。

**本机实测**（body = 重复的 `-----BEGIN RSA PRIVATE KEY-----`，无 END）

| body 大小 | 标记数 | 耗时 |
|---|---|---|
| 1.5 KB | 50 | 4.4 ms |
| 3.0 KB | 100 | 43.5 ms |
| 6.1 KB | 200 | 372.6 ms |
| 12.1 KB | 400 | **2844.9 ms** |

每翻倍 ×10 → 典型 O(n²)。`_audit_response` 在响应钩子里**同步**执行，
上游回一个 `400` + 这种 body 就能让 mitmproxy 事件循环 CPU 打满（512KB 量级已是小时级）。
`error_leak` 默认开启。

> 注：本次新增的注入检测正则（`_INSTRUCTION_OVERRIDE_RE` 等）经实测为线性，**无灾难性回溯**；
> 真正的回溯风险只在旧有的 PEM 正则。

### M2 `/api/ext/restore` 缺体积闸门

`/api/ext/mask`（`panel.py:5679`）与 `/api/ext/mask-file`（`panel.py:5789`）都有
`_EXT_MAX_BODY` + `Transfer-Encoding: chunked` 双判据；`/api/ext/restore`（`panel.py:5835` 起）**两者都没有**。
且 `ext_frames[stream_id]`（`transparent.py:472 / 5069-5071`）是客户端可控的字典、无条目数上限。
启用站点上的任意脚本可构造大量 `stream_id`，在会话 TTL 内撑大引擎内存。

### M3 扩展令牌可被发往任意外域

- `extension/options.js:413-421`（`testConnection`）：把输入框里的**任意 URL** 直接
  `chrome.storage.local.set({ panelUrl, token })` 后触发 ping，**无任何 host 校验**；
- 而同一个地址的「保存设置」按钮（`options.js:482-489`）**有**正则校验
  `/^https?:\/\/(127\.0\.0\.1|localhost)(:\d+)?$/i`；
- `extension/background.js:135-138`（`getPanelUrl`）与 `306-317`（`callPanel`）对 `panelUrl`
  **零校验**，直接带 `X-Shield-Token` 请求。

**纵深防御缺口**：唯一的 host 校验在保存按钮里，而**真正持有令牌的 SW 不自校验**。
用户在「引擎地址」框填外域后点「测试连接」（该按钮就贴在地址框右侧）即触发；
令牌泄漏需目标服务器满足 CORS 预检 —— 恶意/受控服务器必然满足。
修复：把 localhost 校验下沉到 `background.js`，并给 `testConnection` 补同一正则（两者缺一不可）。

### M4 ≤80 字符的写请求整体跳过打码

`extension/bridge-main.js` `MIN_MASKABLE_LEN = 80`。方法已在前面按 `POST/PUT/PATCH` 过滤，
该阈值的原始理由（滤掉 GET 查询串）已不成立，现在只剩一个效果：**≤80 字符的写请求不打码、不记录、无提示**。
与 multipart 分支（无长度阈值）不一致，属「静默未脱敏」。

### M5 24–25MB Office 附件必然硬失败

- `extension/bridge-main.js:193` 允许单文件 ≤25MB；
- `bridge-main.js:319-321` 整文件 base64 后一次性发 `/api/ext/mask-file`；
- base64 膨胀 4/3 → 25MB ≈ 33.3MB > `panel._EXT_MAX_BODY`(32MB) → `413 blocking:true` →
  扩展归 (A) 类 → 页面**网络错误**（不是降级直通）。

### M6 NER 部分结果被写入 LRU 缓存 → 持续欠脱敏

`engine/ner_engine.py:292-294`（预算超时 `break`）与 `231-235`（单块推理异常 `break`）
都会带着**不完整**的实体列表走到 `402` 行：

```python
    _CACHE[text] = [dict(e) for e in final_merged]      # 无条件缓存
```
该文本此后命中缓存直接返回残缺结果，即使系统空闲也不再补全。
同源问题：`328-329` 空结果**提前 return**（不写负缓存）→ 无实体的长文本每次请求重复推理
（≤2000 字约 500ms/次，实测成本模型 0.25ms/字）。

### M7 NER「超长整条跳过」在 UI 不可见

`MAX_TEXT_CHARS = 2000`（`ner_engine.py:39`）：单个字符串叶子超过 2000 字**整条不做语义识别**。
`_SKIP_STATS` 有 `too_long` / `budget_exhausted` / `inference_failed` 计数，
但 `_ner_status_payload`（`panel.py:5620-5642`）只回 `enabled/available/initialized/reason`，
**不含 `skips`**；前端 `Settings.tsx:1849` 也只看 `available`。
→ 「开了 NER，长文本全跳过」在界面上完全看不出，只剩一条进程级一次性日志。

---

## 四、低危

| # | 问题 | 位置 |
|---|---|---|
| L1 | 审计侧 SSE 解析要求 `data: ` **带空格**（规范中空格可选）→ 部分上游的 S2/S4 审计静默失效（还原路径用的是不带空格的写法，不受影响） | `transparent.py:3701` |
| L2 | 重叠裁剪只按起点丢弃（`if ent["start"] < prev_end: continue`），注释断言「不会漏明文」**不严格成立**：前者 `end` 被后缀扩充推过后者 `start`、而后者 `end` 更远时，尾部区间不属于任何实体 → 不打码。窄路径，需构造用例确认触发率 | `ner_engine.py:336 / 382` |
| L3 | `GPE → ADDR`：国家/城市名（「北京」「中国」）被当地址脱敏 → 过度脱敏、词榜噪声化 | `ner_engine.py:104/196` |
| L4 | `lbl = _ID2LABEL.get(str(pred),"O")` 后直接 `lbl[0]` / `lbl[2:]`：`config.json` 被替换成含空标签时 IndexError（当前模型不受影响） | `ner_engine.py:241/253` |
| L5 | NER 模型 99MB **不在仓库、CI 不下载**：GitHub runner 上 PyInstaller 构建的官方安装包**不含模型** → 下载安装的用户启用 NER 只会看到「模型文件缺失」。`build.ps1:268` 只告警不终止（有意识取舍），但 CHANGELOG 宣传「明文残留 74%→11%」未标注该前提。Docker 同理（`.dockerignore` 未排除 `engine/models/`，镜像 +99MB 且未装 onnxruntime） | `maskit-engine.spec:77`、`build.ps1:268`、`.dockerignore` |
| L6 | 审计前端查询无 `isError` 处理：后端 5xx 时 `events=[]` → 渲染成**绿色「未发现异常」空态**，把「没读到」显示成「没问题」 | `frontend/src/pages/Audit.tsx:130-136 / 431-452` |
| L7 | 注入检测误报：讲解 ChatML 模板（写出成对标记 `<|im_start|>system … <|im_end|>`）即报 `fake_system_block` MEDIUM；正常 base64 载荷解码后命中英文索要句式即报 `encoded_instruction` MEDIUM | `audit_signals.py:829-838 / 900-913` |
| L8 | 清空审计无二次确认（误点即丢全部留痕）；前端固定拉 500 条 + 纯客户端过滤，服务端 `floor`/`signal` 参数未用 | `Audit.tsx:132/185`、`panel.py:4774` |

---

## 五、已确认无问题（重点核对项，避免重复排查）

**控制面安全**
- `api_guard` 三重校验覆盖全部 `/api/*`；`//api/config`、`/API/config`、`/api%2Fconfig`、`/api/../api/config` 经归一化后**均无法绕过**（实测）。
- `/api/ext/*` 用**精确白名单**（4 个路径，非前缀）+ `secrets.compare_digest`；`ext_token` 打 `/api/config`、`/api/ext/rotate-token` 必 403；`Origin: null` 被拒；扩展 scheme 只对 4 个端点放行且不外溢到控制面（有测试钉死）。
- `ext_token` 为空时恒 403，不存在「没设 token 就全放行」。

**脱敏与还原正确性**
- `_splice_mask` 的等价校验失败时退回的是 `json.dumps(已脱敏的树)`，**绝不退回原文**。
- 32MB 闸门在 `json.loads` 前拒绝，且对 `Transfer-Encoding: chunked` 同样生效；异常 → `FAIL_CLOSED 503`。
- 流式还原：跨 chunk / 跨通道独立缓冲、`final` 冲刷、无模板时包成合法 `data:` 行、孤儿占位符不猜（有 T1-T4 用例）。
- 后缀撞车置 `_SUFFIX_AMBIGUOUS` 退出兜底，`_suffix_real_token` 要求标签相等 → 不会「A 的值填到 B 的位置」。
- 导出与诊断包剔除原文（白名单 + `_diag_scrub_word_lists`）；`_mask_event_items` 凭据类只落 `digest + preview`。

**审计模块**
- 凭据证据只存 `kind / len / sha256`；`env_var` 只存变量名；`exfil_url` 只存 host+len+sha256。
- `_never_raises` 按 `scan_*` 前缀自动包裹，异常返回空结果，不污染流量。
- `dedupe_findings` 按 `(kind, evidence)` 合并计数，防逐 chunk 刷屏。
- S7 跨请求隔离逻辑正确（canary 转发前剥离，seed 回显不计）。
- 主动探针：并发有锁、未开 `active_probes` 时拒绝扫描（避免假报告）、发送失败判 INCONCLUSIVE。

**扩展链路**
- 令牌生成 `secrets.token_urlsafe(24)`、常量时间比较、从不进 `postMessage`；还原方向恒透传（失败返回原文）。
- 无遥测、无 `storage.sync`、无 `console.log(body)`；`host_permissions` 仅本机；静态门禁覆盖 i18n 键集/脚本顺序/`runAt` 驼峰/DOM id 存在性。
- 动态脚本注册有并发锁（修掉了重复脚本 ID 导致的静默不注入）。

---

## 六、三块功能的完整流程与当前状态

### 1. 浏览器扩展链路

```
页面 fetch/XHR
  → bridge-main.js（MAIN world，hook fetch；XHR 整体透传）
      · 方法过滤 POST/PUT/PATCH → URL 白名单（精准模式：已知对话接口；广泛模式：同站点）
      · 长度闸门 MIN_MASKABLE_LEN=80 / 附件 25MB / OOXML 单文件 25MB
  → window.postMessage（nonce + origin 校验）
  → bridge-isolated.js（ISOLATED world，内容脚本）
  → chrome.runtime.sendMessage → background.js（SW）
      · siteCovers 站点白名单（配置是唯一真相来源）
      · safeCall('/api/ext/mask' | 'mask-file' | 'restore')，带 X-Shield-Token，localhost 校验（缺，见 M3）
  → 引擎 panel.py /api/ext/*
      · api_guard：Host / Origin / Token 三重校验 + 32MB 闸门（restore 缺，见 M2）
      · _EXT_LOCK 内：_new_session → mask_body / restore_stream_chunk
      · _emit(MASK|RESTORE, ingress="ext", dialog=…, *_preview=…)   ← 未清洗（B1）
  → 响应逐 chunk 回页面；还原方向恒透传
```

**状态**：架构与鉴权设计扎实（令牌/白名单/Origin/恒透传/半帧缓冲/失败留痕都做对了）。
**必须先修**：B1（凭据落库）、B3（总开关）、M2、M3、M4、M5。

### 2. 内置模型（NER）识别

```
mask(text, sid)（transparent.py:2786+）
  → 确定性规则（19 项内置 + 自定义词表）先跑
  → NER_ENABLED 时：is_ner_available() → extract_entities(text)
      · MAX_TEXT_CHARS=2000 硬上限 / CALL_BUDGET_S=2.0 / _CACHE(256, LRU)
      · _decode_chunks：CHUNK_SIZE=400 + STRIDE=350 滑窗，BIO/BMES 解码
      · 去重 → ORG/ADDR 后缀扩充 → 重叠裁剪 → 相邻 ADDR 缝合
      · GPE/LOC/FAC → ADDR；PERSON → NAME；ORG → ORG
  → _ner_entity_spans → 按区间替换（与占位符边界切开）→ _mask_by_spans
  → Office 文档：panel.mask_ooxml_bytes 逐 run 调用，整体套 _ner_doc_budget(8s)
还原侧不需要 NER（只做占位符 → 原文查表），设计正确。
```

**状态**：审计期间刚被大幅加固（长度/预算/缓存/失败可见性/打包/依赖/测试 skip 守卫均已修）。
**仍需处理**：M6（部分结果入缓存）、M7（跳过不可见）、L2、L3、L5（分发前提未在文档说明）。
**注意**：CI 只装 `requirements.txt`（flask + mitmproxy），`onnxruntime`/`tokenizers` 在
`requirements-dev.txt` —— 因此 **CI 里 NER 用例全部 skip，该功能在 CI 上零覆盖**（本次本地门禁同样是 2 skipped）。

### 3. API 调用与安全审计模块

```
请求侧：脱敏后转发；X-Shield-Canaries 注入探针 nonce（转发前剥离）
响应侧：restore 完成后 → _audit_response(flow, …)（只读不改 body，异常静默）
  S1 error_leak        ← status≥400 时扫错误页/响应头（PEM 正则 O(n²)，见 M1）
  S2 identity_swap     ← 请求 model vs 响应 model（被 text_chunks 门控，见 B4）
  S3 dangerous_action / S4 sse_anomaly / S7 cross_request_pollution
  注入检测：fake_system_block / instruction_override / prompt_extract /
            credential_exfil / encoded_instruction（载荷同现门控）
  → findings → dedupe_findings → event_store.append_audit_event → /api/audit/events → Audit.tsx
主动探针（audit_engine.py）：跨请求隔离/投毒，profile=general|web3|full → 风险矩阵
```

**状态**：架构取向成熟（形状判据、回声抑制、分档降噪、主动探针 + 覆盖度判定），
凭据红线与「永不抛异常」两条硬约束真正守住了。
**必须先修**：B4。**应修**：M1、L6、L7。

---

## 七、发版前 Checklist

**阻断（必须修 + 必须补回归）**
1. `panel.py` 两处 `_emit` 前补凭据清洗（RESTORE 加 `_redact_session_credentials`）；`api_logs` 非 slim 补读侧兜底。
   → 回归：mask/restore 后断言事件 payload 中不含凭据原文。
2. `_mask_tree` 覆盖数值型标量 + 重复键（`object_pairs_hook`）+ 至少确定性规则的键名；
   → 回归：本报告第二节 B2 表格里 4 种形态逐条断言明文残留 = False。
3. `background.js` 的 `handleMask`/`handleMaskFile` 入口加 `if (!cfg.enabled) return {ok:false, passthrough:true}`。
4. `transparent.py:3618` 把 `scan_identity_swap` 移出循环、调用一次（`chunks or [""]` 那处测试同步改成端到端断言 tool_use-only 响应也能报换芯）。

**建议同版修（非阻断但成本低）**
5. PEM 正则去回溯 + `_audit_response` 入口对 `body_text` 施加与 `_SCAN_BODY_MAX` 一致的截断。
6. `/api/ext/restore` 补 `_EXT_MAX_BODY` + chunked 判据；`ext_frames` 加 stream_id 条数上限。
7. `background.js:getPanelUrl/callPanel` 加 localhost 校验；`options.js:testConnection` 补同一正则。
8. 审计 SSE 解析改为 `startswith("data:")`（一行）。
9. NER：部分结果不入缓存、空结果做负缓存、`_ner_status_payload` 透出 `skips` 并在设置页展示。
10. `Audit.tsx` 补 `isError` 独立错误态，禁止把加载失败渲染成绿色「安全」空态。

**流程要求**
11. **先冻结一版 commit 再复核**（本次审计期间 `engine/*.py` 被实时改写三次，行号与结论存在漂移风险）。
12. CHANGELOG 的 NER 条目补一句前提：官方 Release 安装包不含模型（仅源码自建/本地打包可用）。
13. 确认 `## [<版本>]` 章节已按发布流程从 `Unreleased` 改名（否则 release-draft job 会 `SystemExit(1)`）。

---

## 八、审计局限

- 未在真 Chrome 中实测 M3（令牌外发是否被 CORS 预检拦下）；代码路径已确证，运行时表现属推断。
- M5 的失败形态由 base64 膨胀算术 + 端点闸门推出，未真机上传 25MB 文件验证。
- L2（NER 重叠裁剪尾部漏打码）为逻辑可证、触发率未实测。
- L5 中「官方 Release 不含模型」由 `release.yml` 的构建步骤 + `.gitignore` + spec 的条件收录共同推出，
  未实跑一次 GitHub Actions。
- 本报告不含对 `frontend/` 视觉层与 `src-tauri/` Rust 壳的审计（不在本次三项范围内）。
